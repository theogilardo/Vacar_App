<h1>Reviews</h1>
<div class="container">
  <div class="row">
    <div class="col-6 offset-3">
      <% @reviews.each do |review| %>
        <p>Rating: <%= review.rating %></p>
        <p>Review: <%= review.text %></p>
      <% end %>
<%= link_to "Add review",  new_request_review_path, {class: "btn btn-primary"} %>
      </div>
    </div>
  </div>
</div>

